##### go from LSOA to TTW
import json
import glob
import csv
import numpy

# data = [] 
# with open("E:\Nquiringminds\HousingMap\data\TTWSalesDataAll.json", 'rb') as data_file:
    # for x in data_file:
        # data.append(x)
# loadJSON = json.loads(data[0])

# for year,value in loadJSON.items(): 
    # for ttw, vals in value.items():
        # loadJSON[year][ttw] = numpy.median(vals)
            
# with open("TTWSalesDataAll.json", 'wb') as outfile:
    # json.dump(loadJSON, outfile)